import { View, Text, StyleSheet } from 'react-native';
import { FlatList } from 'react-native';
const Geners = ({ item,navigation }) => {
    let list = item.genres;
    return (
      <View style={styles.genresContainer}>
        
      </View>
    )
};



export default Geners;